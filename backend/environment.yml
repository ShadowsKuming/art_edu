name: artbloom
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - fastapi==0.115.5
    - uvicorn[standard]==0.32.1
    - httpx==0.27.2
    - python-dotenv==1.0.1
    - pydantic==2.9.2
    # §35/§23 — R2 (S3-compatible) client for durable media re-host.
    # NOTE: this file is the conda convenience env and has drifted from
    # requirements.txt (the source of truth for Render). If you run the
    # backend locally via conda, install from requirements.txt instead.
    - boto3==1.35.74
