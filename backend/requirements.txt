fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
# edge-tts ≥ 7.2.x ships the new sec-MS-GEC token algorithm that
# Microsoft started enforcing late 2024. Older 7.0.x builds get 403
# from production data-center IPs. Keep pinned ≥ 7.2.8 (latest stable).
edge-tts==7.2.8
# 2026-05 — boto3 is the S3-compatible client we use to persist per-user
# state JSON blobs to Cloudflare R2. R2 speaks the S3 API verbatim;
# pointing boto3 at an `endpoint_url=https://<account>.r2.cloudflarestorage.com`
# is all the configuration that's needed beyond the usual access key /
# secret pair. See section 10 of main.py for the load/save endpoints.
boto3==1.35.74
