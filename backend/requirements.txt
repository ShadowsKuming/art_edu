fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
edge-tts==7.2.8
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
python-jose[cryptography]==3.3.0
# §35/§23 — R2 (S3-compatible) client for durable user-state + AI-generated
# media. main.py imports boto3 in a try/except and silently falls back to
# ephemeral Ark URLs when it's missing — so without this line the durable
# media re-host NEVER runs in production. botocore is pulled in transitively.
boto3==1.35.74
