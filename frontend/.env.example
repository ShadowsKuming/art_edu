# ─────────────────────────────────────────────────────────────────────
#  ArtBloom frontend environment variables
#  Copy to `.env.local` for dev overrides; on Cloudflare Pages set
#  these in the project's Settings → Environment Variables (Production).
# ─────────────────────────────────────────────────────────────────────

# Backend API base URL.
#   • Dev:        leave UNSET or set to http://localhost:8001
#                 (Vite proxy forwards /api to localhost:8001)
#   • Production: https://artbloom-api.onrender.com
VITE_API_BASE=http://localhost:8001

# Textbook assets host.
#   Textbook images live in public/textbook-assets/ and are served at
#   root-relative /textbook-assets/... paths by both Vite (dev) and
#   Cloudflare Pages (production). Leave UNSET or empty.
#   Only set this if you move assets to a separate CDN/R2 bucket.
# VITE_ASSETS_BASE=https://pub-xxxxxxxx.r2.dev
